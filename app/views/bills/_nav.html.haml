- embed_disqus("bill-#{@bill.id}")

%nav.grid_4
  #left_nav
    = render_navigation(:level => 3)

  %ul
    %li.discuss
      %a= link_to('Comments', discuss_bill_path(@bill.session, @bill, :anchor => 'disqus_thread'))
