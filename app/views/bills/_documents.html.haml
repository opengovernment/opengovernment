.grid_8
  .grid_4.alpha
    %ul.bill_documents
      = render :partial => 'documents/document', :collection => @documents[0...@documents.size/2]

  .grid_4.omega
    %ul.bill_documents
      = render :partial => 'documents/document', :collection => @documents[@documents.size/2..@documents.size]

.documents_more_link.grid_8
  %br(clear='both')
  = paginate @documents, :remote => true, :theme => 'show_more'
-#  - if !@documents.current_page == @documents.last_page
-#    %strong= link_to('Show More', {:page => @documents.page + 1}, :remote => true)
-#    = paginate @documents
  -# Hack to support ajax in pagination links.
-#  :javascript
-#    $('.pagination a').attr('data-remote', 'true');
