- title(@subject.name)

= render :partial => 'shared/left_nav_toplevel'

%h1= @subject.name
%div.grid_12
  = will_paginate @subject_bills
  %ul
    %li
    - @subject_bills.each do |bill|
      = render bill

  = will_paginate @subject_bills
