- title("#{@state.name} Issues")



= render :partial => 'shared/appeal'

%h1
  = "#{@state.name} Issues"

%hr

%p.intro= t '.intro'

%p.intro 

%hr

-# %h2 Project Vote Smart Issue Areas

-# %ul.lined
-#  = render(:partial => 'shared/issue', :collection => @issues)

%h2 Bill Subjects
%ul.lined
  = render @subjects

= link_to(raw('View More Popular Bill Subjects and View All Subjects &rarr;'), subjects_path, :class => 'more')
