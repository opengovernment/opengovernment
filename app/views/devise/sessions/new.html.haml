%h2 Sign in
%p
  Use your OpenCongress login to sign in, or
  %a{:href => 'http://www.opencongress.org/register'} sign up.
  
= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
  %p
    = f.label :login
    %br/
    = f.text_field :email
  %p
    = f.label :password
    %br/
    = f.password_field :password
  - if devise_mapping.rememberable?
    %p
      = f.check_box :remember_me
      = f.label :remember_me
  %p= f.submit "Sign in"
= render :partial => "devise/shared/links"
