- content_for :content do

  %hr.grid_16

  %nav.grid_4
    #left_nav
      = render_navigation(:level => 2) || raw('&nbsp;')

  .grid_12
    - if content_for?(:title)
      %h1.grid_13.alpha= yield(:title)
    = yield

= render :file => 'layouts/application'
