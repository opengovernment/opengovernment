- if @contributions.empty?
  %p Sorry, no contributions matched.
- else
  .search_results
    %ul.contributions
      - @contributions.each do |contribution|
        %li[contribution]
          != contribution.contributor_name

    = paginate @contributions
